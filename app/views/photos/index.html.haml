%h1 Listing photos

%table.table
  %tr
    %th Name
    %th Description
    %th Image
    %th

  - @photos.each do |photo|
    %tr
      %td= photo.name
      %td= photo.description
      %td= photo.image
      %td
        = link_to 'Show', photo, :class => 'btn btn-mini'
        = link_to 'Edit', edit_photo_path(photo), :class => 'btn btn-mini'
        = link_to 'Destroy', photo, :confirm => 'Are you sure?', :method => :delete, :class => 'btn btn-mini btn-danger'

%br

= link_to 'New Photo', new_photo_path
